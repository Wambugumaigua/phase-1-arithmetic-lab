const multiply = (num1, num2) => num1 * num2;
const num1 = 5;
const num2 = 12;
console.log(`The product of ${num1} and ${num2} is ${multiply(num1, num2)}.`);

const random = Math.floor(Math.random() * 10) + 1;
console.log(`The random integer is ${random}.`);

const mod = num3 % num4;
const num3 = 20;
const num4 = 4;
console.log(`The remainder of ${num3} divided by ${num4} is ${mod}.`);

const max = Math.max(10, 20, 30, 40, 50);
console.log(`The maximum number is ${max}.`);